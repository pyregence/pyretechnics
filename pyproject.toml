[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyretechnics"
version = "2024.5.2"

[tool.pytest.ini_options]
addopts = ["--import-mode=importlib"]
pythonpath = "src"
